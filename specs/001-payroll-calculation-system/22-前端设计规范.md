# PayrollMaster前端设计规范 - 现代金融仪表板美学

## 设计概述

PayrollMaster前端采用Next.js 14框架实现，遵循现代金融仪表板美学设计理念。设计强调专业性、易用性和视觉层次感，通过柔和的阴影、圆角设计和优雅的配色方案，营造出值得信赖的企业级应用体验。

## 设计哲学

### 核心原则

1. **清晰性** - 信息层级分明，关键数据突出显示
2. **一致性** - 统一的设计语言和交互模式
3. **效率性** - 最小化操作步骤，快速完成任务
4. **可访问性** - 支持键盘导航、屏幕阅读器等无障碍功能
5. **响应式** - 适配桌面、平板和移动设备

### 金融仪表板美学特征

- **微妙的阴影效果**：营造层次感和深度
- **充足的留白**：减少视觉疲劳，突出重点内容
- **数据可视化优先**：图表和表格清晰易读
- **状态反馈明确**：加载、成功、错误状态清晰区分
- **渐变和色彩**：柔和的渐变增强视觉吸引力

## 视觉设计系统

### 配色方案

#### 主色调（Primary Colors）

```css
/* 主品牌色 - 蓝色系 */
--primary-50: #eff6ff
--primary-100: #dbeafe
--primary-200: #bfdbfe
--primary-300: #93c5fd
--primary-400: #60a5fa
--primary-500: #3b82f6  /* 主色 */
--primary-600: #2563eb
--primary-700: #1d4ed8
--primary-800: #1e40af
--primary-900: #1e3a8a
```

#### 辅助色（Secondary Colors）

```css
/* 成功 - 绿色 */
--success-500: #10b981
--success-600: #059669

/* 警告 - 黄色 */
--warning-500: #f59e0b
--warning-600: #d97706

/* 错误 - 红色 */
--error-500: #ef4444
--error-600: #dc2626

/* 信息 - 紫色 */
--info-500: #8b5cf6
--info-600: #7c3aed
```

#### 中性色（Neutral Colors）

```css
/* 灰度色系 */
--gray-50: #f9fafb
--gray-100: #f3f4f6
--gray-200: #e5e7eb
--gray-300: #d1d5db
--gray-400: #9ca3af
--gray-500: #6b7280
--gray-600: #4b5563
--gray-700: #374151
--gray-800: #1f2937
--gray-900: #111827
```

#### 背景色

```css
/* 背景渐变 */
--bg-gradient-start: #667eea
--bg-gradient-end: #764ba2

/* 卡片背景 */
--card-bg: #ffffff
--card-bg-secondary: #f9fafb

/* 侧边栏背景 */
--sidebar-bg: #1e293b
--sidebar-bg-hover: #334155
```

### 字体系统

```css
/* 字体族 */
--font-sans: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif
--font-mono: 'JetBrains Mono', 'Fira Code', monospace

/* 字号层级 */
--text-xs: 0.75rem     /* 12px */
--text-sm: 0.875rem    /* 14px */
--text-base: 1rem      /* 16px */
--text-lg: 1.125rem    /* 18px */
--text-xl: 1.25rem     /* 20px */
--text-2xl: 1.5rem     /* 24px */
--text-3xl: 1.875rem   /* 30px */
--text-4xl: 2.25rem    /* 36px */

/* 字重 */
--font-normal: 400
--font-medium: 500
--font-semibold: 600
--font-bold: 700
```

### 圆角系统

```css
/* 圆角尺寸 */
--radius-sm: 0.25rem   /* 4px */
--radius-md: 0.375rem  /* 6px */
--radius-lg: 0.5rem    /* 8px */
--radius-xl: 0.75rem   /* 12px */
--radius-2xl: 1rem     /* 16px */
--radius-full: 9999px  /* 完全圆形 */
```

### 阴影系统

```css
/* 阴影层级 */
--shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05)
--shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1)
--shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)
--shadow-xl: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1)
--shadow-2xl: 0 25px 50px -12px rgb(0 0 0 / 0.25)
--shadow-inner: inset 0 2px 4px 0 rgb(0 0 0 / 0.05)

/* 彩色阴影 */
--shadow-blue: 0 10px 25px -5px rgba(59, 130, 246, 0.3)
--shadow-green: 0 10px 25px -5px rgba(16, 185, 129, 0.3)
--shadow-purple: 0 10px 25px -5px rgba(139, 92, 246, 0.3)
```

### 间距系统

```css
/* 间距单位（基于8px网格） */
--space-1: 0.25rem   /* 4px */
--space-2: 0.5rem    /* 8px */
--space-3: 0.75rem   /* 12px */
--space-4: 1rem      /* 16px */
--space-5: 1.25rem   /* 20px */
--space-6: 1.5rem    /* 24px */
--space-8: 2rem      /* 32px */
--space-10: 2.5rem   /* 40px */
--space-12: 3rem     /* 48px */
--space-16: 4rem     /* 64px */
--space-20: 5rem     /* 80px */
```

## 布局架构

### 整体布局

```
┌─────────────────────────────────────────────────────────────┐
│                        顶部导航栏                           │
│                      (64px 高度)                           │
├──────────────┬──────────────────────────────────────────────┤
│              │                                              │
│              │                                              │
│              │                                              │
│   侧边栏      │              主内容区                        │
│  (260px 宽)  │            (自适应宽度)                      │
│              │                                              │
│              │                                              │
│              │                                              │
│              │                                              │
│              │                                              │
└──────────────┴──────────────────────────────────────────────┘
```

### 响应式断点

```css
/* 断点定义 */
--breakpoint-sm: 640px
--breakpoint-md: 768px
--breakpoint-lg: 1024px
--breakpoint-xl: 1280px
--breakpoint-2xl: 1536px

/* 响应式行为 */
- sm: 移动设备横屏
- md: 平板设备
- lg: 笔记本
- xl: 桌面显示器
- 2xl: 大屏显示器
```

### 侧边栏导航

#### 状态设计

**折叠状态** (76px宽)
- 仅显示图标
- 悬浮展开子菜单

**展开状态** (260px宽)
- 显示完整菜单项
- 包含图标、标题和徽章

#### 菜单项结构

```typescript
interface MenuItem {
  id: string
  label: string
  icon: React.ComponentType
  path?: string
  children?: MenuItem[]
  badge?: number | string
  disabled?: boolean
}
```

#### 菜单层级

```
📊 仪表板 (Dashboard)
  └── 总览

👥 员工管理 (Employee Management)
  ├── 员工列表
  └── 导入数据

💰 薪资管理 (Payroll Management)
  ├── 薪资规则
  ├── 薪资计算
  └── 计算结果

📈 报表中心 (Reports)
  ├── 员工报表
  ├── 薪资报表
  └── 自定义报表

⚙️ 系统设置 (Settings)
  ├── 用户管理
  ├── 角色权限
  └── 数据备份

📋 操作日志 (Logs)
  └── 系统日志
```

## 页面设计规范

### 1. 登录页面 (Login Page)

#### 设计特点

- **居中卡片布局**：宽400px，白色卡片，shadow-2xl
- **渐变背景**：蓝色到紫色渐变
- **品牌标识**：PayrollMaster Logo和标语
- **表单设计**：
  - 输入框：圆角-lg，border-gray-200，focus状态蓝色边框
  - 按钮：primary-500渐变，圆角-lg，shadow-md
  - 记住我：开关样式设计
- **辅助链接**：忘记密码、注册链接

#### 视觉示例

```
┌─────────────────────────────────────────┐
│                                         │
│           PayrollMaster                 │
│        薪资自动核算系统                  │
│                                         │
│  ┌─────────────────────────────────────┐ │
│  │  用户名                             │ │
│  └─────────────────────────────────────┘ │
│  ┌─────────────────────────────────────┐ │
│  │  密码                               │ │
│  └─────────────────────────────────────┘ │
│  □ 记住我                               │
│                                         │
│  ┌─────────────────────────────────────┐ │
│  │        登录 (蓝色渐变按钮)           │ │
│  └─────────────────────────────────────┘ │
│                                         │
│     忘记密码？   注册账号？              │
└─────────────────────────────────────────┘
```

### 2. 仪表板页面 (Dashboard)

#### 布局结构

```
┌─────────────────────────────────────────────────────────┐
│                        面包屑导航                        │
├──────────────┬───────────────────────────────────────────┤
│              │                                           │
│              │  ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│              │  │ 总员工数 │ │ 本月核算 │ │ 待审核   │  │
│              │  │   1,256  │ │ ¥458,200 │ │    23    │  │
│              │  └──────────┘ └──────────┘ └──────────┘  │
│   侧边栏      │                                           │
│              │  ┌─────────────────────────────────────┐  │
│              │  │        薪资趋势图表                 │  │
│              │  │     (Line Chart 折线图)            │  │
│              │  └─────────────────────────────────────┘  │
│              │                                           │
│              │  ┌─────────────────┐ ┌─────────────────┐ │
│              │  │  最新计算记录   │ │  待处理事项     │ │
│              │  │   (Table)      │ │   (List)       │ │
│              │  └─────────────────┘ └─────────────────┘ │
└──────────────┴───────────────────────────────────────────┘
```

#### 统计卡片组件

```typescript
interface StatCardProps {
  title: string
  value: string | number
  icon: React.ComponentType
  trend?: {
    value: number
    isPositive: boolean
  }
  color?: 'blue' | 'green' | 'purple' | 'yellow'
}
```

**视觉特征**：
- 背景：白色卡片，radius-xl
- 阴影：shadow-lg
- 图标：左侧，彩色圆形背景
- 数值：大号字体 (text-3xl)，加粗
- 趋势：右侧，绿色/红色箭头和百分比

### 3. 员工列表页面

#### 布局结构

```
┌─────────────────────────────────────────────────────────┐
│                    页面标题 + 操作按钮                   │
│  员工管理                    [+ 新增] [📤 导入]        │
├──────────────┬───────────────────────────────────────────┤
│              │                                           │
│   筛选面板    │  ┌─────────────────────────────────────┐  │
│              │  │ 🔍 搜索框                            │  │
│  ┌─────────┐  │  └─────────────────────────────────────┘  │
│  │ 部门筛选 │  │                                           │
│  └─────────┘  │  ┌─────────────────────────────────────┐  │
│               │  │                                     │  │
│  ┌─────────┐  │  │           数据表格                   │  │
│  │ 状态筛选 │  │  │                                     │  │
│  └─────────┘  │  │                                     │  │
│               │  └─────────────────────────────────────┘  │
│  ┌─────────┐  │                                           │
│  │ 重置   │  │            分页控件                        │
│  └─────────┘  │                                           │
└──────────────┴───────────────────────────────────────────┘
```

#### 数据表格设计

**表头**：
- 背景：gray-50
- 文字：gray-700，font-semibold
- 排序图标：右侧，hover时显示

**表格行**：
- hover：gray-50背景
- 圆角：radius-lg（首行和末行）
- 内边距：py-4 px-6

**操作按钮**：
- 查看：蓝色文字
- 编辑：绿色文字
- 删除：红色文字

#### Excel文件格式说明

**重要提醒**：Excel文件格式采用双层表头设计

```
第1行：英文字段名   employeeId | name | department | position | basicSalary
第2行：中文字段名   员工工号   | 姓名  | 部门       | 职位     | 基本薪资
第3行：───────────|──────────|──────|────────────|──────────|──────────
第4行：实际数据     EMP001    | 张三  | 技术部      | 工程师   | 15000
```

**解析逻辑**：
1. 读取前两行作为字段映射表
2. 第一行（英文）用于数据库字段映射
3. 第二行（中文）用于用户界面显示
4. 从第3行开始解析实际数据

**代码实现**：
```typescript
const parseExcelHeaders = (worksheet: any) => {
  const headers = []
  for (let col = 1; col <= worksheet.maxColumn; col++) {
    const englishName = worksheet.getCell(1, col).value
    const chineseName = worksheet.getCell(2, col).value
    headers.push({
      english: englishName,
      chinese: chineseName,
      key: englishName.toLowerCase().replace(/[^a-z0-9]/g, '_')
    })
  }
  return headers
}
```

### 4. 薪资计算页面

#### 布局结构

```
┌─────────────────────────────────────────────────────────┐
│                      配置面板                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐    │
│  │计算日期  │ │ 部门     │ │ 规则选择 │ │ 预览模式 │    │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘    │
│                 [🚀 开始计算]                           │
├──────────────┬───────────────────────────────────────────┤
│              │                                           │
│   侧边栏      │  ┌─────────────────────────────────────┐  │
│              │  │         计算进度显示                 │  │
│              │  │                                     │  │
│              │  │  ████████████░░░░  75%              │  │
│              │  │                                     │  │
│              │  │  正在计算: 张三 (技术部)             │  │
│              │  │                                     │  │
│              │  └─────────────────────────────────────┘  │
│              │                                           │
│              │  ┌─────────────────────────────────────┐  │
│              │  │         实时日志                    │  │
│              │  │                                     │  │
│              │  └─────────────────────────────────────┘  │
└──────────────┴───────────────────────────────────────────┘
```

#### 计算规则配置组件

```typescript
interface RuleSelectorProps {
  rules: PayrollRule[]
  selectedRules: number[]
  onChange: (rules: number[]) => void
}
```

### 5. 报表页面

#### 布局结构

```
┌─────────────────────────────────────────────────────────┐
│                     报表筛选器                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐    │
│  │报表类型  │ │ 时间范围 │ │ 部门     │ │ [导出]  │    │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘    │
├──────────────┬───────────────────────────────────────────┤
│              │                                           │
│   侧边栏      │  ┌─────────────────────────────────────┐  │
│              │  │                                     │  │
│              │  │            报表图表                 │  │
│              │  │        (多种图表类型)               │  │
│              │  │                                     │  │
│              │  └─────────────────────────────────────┘  │
│              │                                           │
│              │  ┌─────────────────────────────────────┐  │
│              │  │                                     │  │
│              │  │            数据表格                 │  │
│              │  │                                     │  │
│              │  └─────────────────────────────────────┘  │
└──────────────┴───────────────────────────────────────────┘
```

## 组件设计规范

### 按钮组件 (Button)

#### 变体类型

```typescript
type ButtonVariant = 'primary' | 'secondary' | 'outline' | 'ghost' | 'danger'

type ButtonSize = 'sm' | 'md' | 'lg' | 'xl'
```

#### 设计规范

**Primary Button**：
- 背景：primary-500 到 primary-600 渐变
- 文字：白色
- 圆角：radius-lg
- 阴影：shadow-md
- Hover：primary-600 渐变，shadow-lg

**Secondary Button**：
- 背景：gray-100
- 文字：gray-700
- 圆角：radius-lg
- Hover：gray-200

**Outline Button**：
- 背景：透明
- 边框：primary-500 (1px)
- 文字：primary-600
- 圆角：radius-lg
- Hover：primary-50 背景

**Ghost Button**：
- 背景：透明
- 文字：gray-600
- 圆角：radius-md
- Hover：gray-100 背景

### 输入框组件 (Input)

#### 设计规范

```typescript
interface InputProps {
  size?: 'sm' | 'md' | 'lg'
  variant?: 'default' | 'filled' | 'flushed'
  error?: boolean
  disabled?: boolean
}
```

**Default Input**：
- 边框：border-gray-200
- 背景：white
- 圆角：radius-lg
- Padding：py-2 px-4
- Focus：border-primary-500，shadow-sm，outline-none

**Filled Input**：
- 背景：gray-100
- 边框：无
- 圆角：radius-lg
- Focus：gray-200 背景，shadow-sm

### 卡片组件 (Card)

#### 设计规范

```typescript
interface CardProps {
  variant?: 'elevated' | 'outlined' | 'ghost'
  padding?: 'none' | 'sm' | 'md' | 'lg' | 'xl'
  radius?: 'none' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'
}
```

**Elevated Card**：
- 背景：white
- 阴影：shadow-lg
- 圆角：radius-xl
- 边框：无

**Outlined Card**：
- 背景：white
- 边框：border-gray-200 (1px)
- 圆角：radius-xl
- 阴影：无

### 表格组件 (Table)

#### 设计规范

```typescript
interface TableProps {
  variant?: 'simple' | 'striped' | 'bordered'
  size?: 'sm' | 'md' | 'lg'
  hoverable?: boolean
}
```

**Simple Table**：
- 表头：gray-50 背景，gray-700 文字，加粗
- 表格行：white 背景
- 边框：仅底部 border-gray-200
- 圆角：radius-lg（容器）

**Striped Table**：
- 奇数行：white 背景
- 偶数行：gray-50 背景
- Hover：gray-100 背景

### 模态框组件 (Modal)

#### 设计规范

```typescript
interface ModalProps {
  size?: 'sm' | 'md' | 'lg' | 'xl' | 'full'
  showCloseButton?: boolean
  closeOnOverlayClick?: boolean
}
```

**Overlay**：
- 背景：rgba(0, 0, 0, 0.5)
- 动画：fadeIn

**Modal Content**：
- 背景：white
- 圆角：radius-2xl
- 阴影：shadow-2xl
- 最大宽度：根据size属性
- 动画：slideUp

### 通知组件 (Toast/Notification)

#### 设计规范

```typescript
interface ToastProps {
  type?: 'success' | 'error' | 'warning' | 'info'
  position?: 'top-right' | 'top-left' | 'bottom-right' | 'bottom-left'
  duration?: number
}
```

**Success Toast**：
- 图标：绿色 check-circle
- 背景：green-50
- 文字：green-800
- 边框：green-200 (1px)

**Error Toast**：
- 图标：红色 x-circle
- 背景：red-50
- 文字：red-800
- 边框：red-200 (1px)

## 交互设计

### 过渡动画

#### 页面过渡

```css
/* 页面进入 */
.page-enter {
  opacity: 0;
  transform: translateY(20px);
}

.page-enter-active {
  opacity: 1;
  transform: translateY(0);
  transition: opacity 300ms, transform 300ms;
}

/* 页面退出 */
.page-exit {
  opacity: 1;
}

.page-exit-active {
  opacity: 0;
  transition: opacity 300ms;
}
```

#### 组件动画

```css
/* 淡入 */
.fade-in {
  animation: fadeIn 0.3s ease-in-out;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

/* 滑入 */
.slide-in-right {
  animation: slideInRight 0.3s ease-out;
}

@keyframes slideInRight {
  from { transform: translateX(100%); }
  to { transform: translateX(0); }
}

/* 缩放 */
.scale-in {
  animation: scaleIn 0.2s ease-out;
}

@keyframes scaleIn {
  from { transform: scale(0.9); opacity: 0; }
  to { transform: scale(1); opacity: 1; }
}
```

### 状态反馈

#### 加载状态

```typescript
// 骨架屏
const SkeletonLoader = () => (
  <div className="animate-pulse">
    <div className="h-4 bg-gray-200 rounded w-3/4 mb-2"></div>
    <div className="h-4 bg-gray-200 rounded w-1/2"></div>
  </div>
)

// 加载指示器
const LoadingSpinner = () => (
  <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-primary-500"></div>
)
```

#### 空状态

```typescript
const EmptyState = ({ icon: Icon, title, description, action }) => (
  <div className="text-center py-12">
    <Icon className="mx-auto h-12 w-12 text-gray-400" />
    <h3 className="mt-2 text-sm font-semibold text-gray-900">{title}</h3>
    <p className="mt-1 text-sm text-gray-500">{description}</p>
    {action && <div className="mt-6">{action}</div>}
  </div>
)
```

#### 错误状态

```typescript
const ErrorState = ({ error, onRetry }) => (
  <div className="text-center py-12">
    <XCircleIcon className="mx-auto h-12 w-12 text-red-500" />
    <h3 className="mt-2 text-sm font-semibold text-gray-900">出现错误</h3>
    <p className="mt-1 text-sm text-gray-500">{error}</p>
    <div className="mt-6">
      <Button onClick={onRetry}>重试</Button>
    </div>
  </div>
)
```

## 数据可视化

### 图表组件

#### 使用Chart.js或Recharts

```typescript
import {
  LineChart,
  Line,
  AreaChart,
  Area,
  BarChart,
  Bar,
  PieChart,
  Pie,
  Cell,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  Legend,
  ResponsiveContainer
} from 'recharts'
```

#### 配色方案

```typescript
const CHART_COLORS = [
  '#3b82f6', // blue-500
  '#10b981', // emerald-500
  '#f59e0b', // amber-500
  '#ef4444', // red-500
  '#8b5cf6', // violet-500
  '#ec4899', // pink-500
  '#14b8a6', // teal-500
  '#f97316', // orange-500
]
```

#### 图表样式

```css
/* 图表容器 */
.chart-container {
  background: white;
  border-radius: 1rem;
  padding: 1.5rem;
  box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
}

/* 坐标轴 */
.recharts-cartesian-axis-tick-value {
  fill: #6b7280;
  font-size: 0.875rem;
}

/* 网格线 */
.recharts-cartesian-grid-horizontal line,
.recharts-cartesian-grid-vertical line {
  stroke: #e5e7eb;
  stroke-dasharray: 3;
}

/* 提示框 */
.recharts-default-tooltip {
  border-radius: 0.5rem !important;
  border: 1px solid #e5e7eb !important;
  box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1) !important;
}
```

## 响应式设计

### 断点策略

#### 移动端 (< 768px)

- 侧边栏：固定定位，slide-in 动画
- 表格：横向滚动
- 卡片：全宽显示
- 导航：汉堡菜单

#### 平板端 (768px - 1024px)

- 侧边栏：可折叠
- 表格：紧凑模式
- 卡片：2列网格

#### 桌面端 (> 1024px)

- 侧边栏：固定展开
- 表格：完整模式
- 卡片：3-4列网格

### 实现示例

```tsx
// 响应式布局
const ResponsiveLayout = ({ children }) => {
  return (
    <div className="flex">
      {/* 侧边栏 */}
      <aside className={`
        fixed inset-y-0 left-0 z-50 w-64 bg-gray-800 transform transition-transform
        lg:translate-x-0 lg:static lg:inset-0
        ${isSidebarOpen ? 'translate-x-0' : '-translate-x-full'}
      `}>
        {/* 侧边栏内容 */}
      </aside>

      {/* 主内容 */}
      <main className="flex-1 lg:ml-0">
        {/* 顶部导航 */}
        <header className="lg:hidden">
          {/* 移动端菜单按钮 */}
        </header>

        {/* 页面内容 */}
        <div className="p-4 lg:p-8">
          {children}
        </div>
      </main>
    </div>
  )
}
```

## 无障碍性设计

### ARIA标签

```tsx
// 按钮
<button
  aria-label="删除员工"
  aria-describedby="delete-tooltip"
  className="text-red-600 hover:text-red-700"
>
  <TrashIcon className="h-5 w-5" />
</button>

// 输入框
<label htmlFor="username" className="sr-only">
  用户名
</label>
<input
  id="username"
  type="text"
  aria-required="true"
  aria-invalid={hasError}
  aria-describedby={hasError ? 'username-error' : undefined}
/>

// 表格
<table
  role="table"
  aria-label="员工列表"
>
  <thead>
    <tr role="row">
      <th role="columnheader" aria-sort="ascending">
        姓名
      </th>
    </tr>
  </thead>
  <tbody>
    <tr role="row">
      <td role="cell">张三</td>
    </tr>
  </tbody>
</table>
```

### 键盘导航

```tsx
// 模态框焦点管理
const Modal = ({ isOpen, onClose }) => {
  const modalRef = useRef<HTMLDivElement>(null)

  useEffect(() => {
    if (isOpen) {
      // 聚焦第一个可交互元素
      const firstFocusable = modalRef.current?.querySelector(
        'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
      ) as HTMLElement
      firstFocusable?.focus()
    }
  }, [isOpen])

  return (
    <div
      role="dialog"
      aria-modal="true"
      ref={modalRef}
      onKeyDown={(e) => {
        if (e.key === 'Escape') {
          onClose()
        }
      }}
    >
      {/* 模态框内容 */}
    </div>
  )
}
```

## Next.js特性应用

### App Router结构

```
app/
├── layout.tsx              # 根布局
├── page.tsx                # 首页 (仪表板)
├── login/
│   └── page.tsx            # 登录页
├── employees/
│   ├── page.tsx            # 员工列表
│   ├── new/
│   │   └── page.tsx        # 新增员工
│   └── [id]/
│       └── page.tsx        # 员工详情
├── payroll/
│   ├── rules/
│   │   └── page.tsx        # 薪资规则
│   ├── calculate/
│   │   └── page.tsx        # 薪资计算
│   └── results/
│       └── page.tsx        # 计算结果
└── api/
    ├── employees/
    │   └── route.ts        # 员工API
    ├── payroll/
    │   ├── rules/
    │   │   └── route.ts    # 规则API
    │   └── calculate/
    │       └── route.ts    # 计算API
    └── auth/
        └── route.ts        # 认证API
```

### 服务器端渲染 (SSR)

```tsx
// 获取服务器端数据
const PayrollResults = async ({ calculationDate }) => {
  // 直接在服务器端获取数据
  const results = await fetch(
    `${process.env.API_URL}/payroll/calculations?date=${calculationDate}`,
    { cache: 'no-store' } // 每次请求都重新获取
  )

  const data = await results.json()

  return (
    <div className="bg-white rounded-xl shadow-lg p-6">
      {/* 渲染数据 */}
    </div>
  )
}
```

### 静态生成 (SSG)

```tsx
// 静态生成的页面
const EmployeeList = async () => {
  // 构建时获取数据
  const employees = await fetch(
    `${process.env.API_URL}/employees`,
    { next: { revalidate: 3600 } } // 每小时重新生成
  )

  const data = await employees.json()

  return (
    <div className="space-y-4">
      {data.map(employee => (
        <EmployeeCard key={employee.id} employee={employee} />
      ))}
    </div>
  )
}
```

### API路由

```tsx
// app/api/payroll/calculate/route.ts
import { NextRequest, NextResponse } from 'next/server'

export async function POST(request: NextRequest) {
  try {
    const body = await request.json()
    const { calculationDate, department, rules } = body

    // 调用后端API
    const result = await fetch(`${process.env.API_URL}/payroll/calculate`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${getToken()}`
      },
      body: JSON.stringify({ calculationDate, department, rules })
    })

    const data = await result.json()

    return NextResponse.json(data)
  } catch (error) {
    return NextResponse.json(
      { error: '计算失败' },
      { status: 500 }
    )
  }
}
```

## 性能优化

### 图片优化

```tsx
import Image from 'next/image'

const CompanyLogo = () => (
  <Image
    src="/logo.png"
    alt="PayrollMaster Logo"
    width={120}
    height={40}
    priority
    className="h-auto"
  />
)
```

### 代码分割

```tsx
// 动态导入组件
const HeavyChart = dynamic(
  () => import('./components/Chart'),
  {
    ssr: false,
    loading: () => <SkeletonLoader />
  }
)
```

### 缓存策略

```tsx
// 使用React Query进行数据缓存
import { useQuery } from '@tanstack/react-query'

const EmployeesPage = () => {
  const { data, isLoading, error } = useQuery({
    queryKey: ['employees'],
    queryFn: fetchEmployees,
    staleTime: 5 * 60 * 1000, // 5分钟内数据保持新鲜
    cacheTime: 10 * 60 * 1000, // 10分钟缓存时间
  })

  // 渲染逻辑
}
```

## 开发规范

### 文件命名

- 组件：PascalCase (如 `PayrollCard.tsx`)
- 页面：kebab-case (如 `payroll-results.tsx`)
- 钩子：camelCase (如 `usePayrollData.ts`)
- 工具：camelCase (如 `formatCurrency.ts`)
- 常量：UPPER_SNAKE_CASE (如 `API_ENDPOINTS.ts`)

### 代码组织

```typescript
// 组件结构示例
interface ComponentProps {
  // props类型定义
}

export const Component: React.FC<ComponentProps> = ({ prop1, prop2 }) => {
  // 1. 钩子
  const [state, setState] = useState()
  const { data } = useQuery()

  // 2. 事件处理
  const handleClick = () => {}

  // 3. 计算属性
  const computedValue = useMemo(() => {}, [])

  // 4. 渲染
  return (
    <div className="...">
      {/* JSX */}
    </div>
  )
}
```

### Tailwind CSS类名顺序

```tsx
// 推荐顺序
<div
  className={[
    // 1. 布局
    'flex',
    // 2. 尺寸
    'w-full h-64',
    // 3. 间距
    'p-4 m-2',
    // 4. 排版
    'text-sm font-medium',
    // 5. 颜色
    'text-gray-700 bg-white',
    // 6. 边框
    'border border-gray-200 rounded-lg',
    // 7. 阴影
    'shadow-md',
    // 8. 其他
    'hover:shadow-lg'
  ].join(' ')}
>
```

## 主题定制

### 深色模式支持

```css
/* tailwind.config.js */
module.exports = {
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        dark: {
          bg: '#0f172a',
          card: '#1e293b',
          text: '#f1f5f9',
        }
      }
    }
  }
}
```

```tsx
// 主题切换组件
const ThemeToggle = () => {
  const [darkMode, setDarkMode] = useState(false)

  useEffect(() => {
    if (darkMode) {
      document.documentElement.classList.add('dark')
    } else {
      document.documentElement.classList.remove('dark')
    }
  }, [darkMode])

  return (
    <button
      onClick={() => setDarkMode(!darkMode)}
      className="p-2 rounded-lg bg-gray-100 dark:bg-gray-800"
    >
      {darkMode ? <SunIcon /> : <MoonIcon />}
    </button>
  )
}
```

## 总结

本设计规范定义了PayrollMaster前端的完整设计体系，涵盖：

✅ **视觉设计系统**：配色、字体、圆角、阴影等
✅ **布局架构**：响应式布局、侧边栏导航
✅ **页面设计**：8个核心页面的详细设计
✅ **组件规范**：按钮、输入框、卡片等组件
✅ **交互设计**：动画、状态反馈
✅ **数据可视化**：图表样式和配色
✅ **响应式设计**：移动端适配策略
✅ **无障碍性**：ARIA标签、键盘导航
✅ **Next.js特性**：App Router、SSR、SSG
✅ **性能优化**：代码分割、缓存策略
✅ **开发规范**：文件命名、代码组织

遵循此规范，可确保PayrollMaster前端界面的一致性、可维护性和优秀的用户体验。
